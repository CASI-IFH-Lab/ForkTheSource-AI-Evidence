#!/usr/bin/env bash
# Regenerate STATUS.md after a merge or a pull. Silent on success, never fails.
# Skips entirely if the merge stopped on a conflict - see _status_common.sh.
set -u
. "$(dirname "$0")/_status_common.sh"
status_hook_run
exit 0
